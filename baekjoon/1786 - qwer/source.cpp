#include <stdio.h>
#include <string.h>

typedef long long ll;

char t[1000010];
char p[1000010];

int main(void){
    scanf("%[^\n]s", t);
    scanf("%[^\n]s", p);
    int lt = strlen(t);
    int lp = strlen(p);
    ll ht = 0, hp = 0;
    for(int i=0;i<lp;i++){
        
    }

    return 0;
}
